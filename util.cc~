// util.cc

#include <rarray>
#include <rarrayio>
#include <iostream>


void print_top_five(rarray<double,2> arr){
  
  rarray<double,2> top_five= zeros(5, 2);  // array to store top correlations and indices
  
  for (int i=0; i<arr.extent(0); i++){
    
    // If current element is greater than the first 
    if (corr[i][1] > 
  }

  
  
}
